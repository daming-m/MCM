spring:
  profiles:
    active: sqlite
  main:
    allow-bean-definition-overriding: true

  thymeleaf:
    cache: false

mybatis:
  mapper-locations: classpath*:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 9090

logging:
  level:
    com.example.uavmanager: INFO

